import { movies } from "../movies";

const initialState = {
    sira: 0,
    movie: movies,
    favMovies: [],
};

const rootReducer = (state = initialState, action) => {};

export default rootReducer;